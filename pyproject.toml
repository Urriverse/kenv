[project]
name = "osenv"
version = "0.1.1"
description = "OS building and running suite."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "git-python>=1.0.0",
    "requests>=2.34.2",
]
